export const metadata = {
  title: "Qidiruv — Minutka",
  description: "Restoranlar va taomlarni qidiring.",
};

export default function SearchLayout({ children }: { children: React.ReactNode }) {
  return children;
}
