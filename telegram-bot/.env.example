# Telegram bot (HTTP /notify + webhook).
TELEGRAM_BOT_TOKEN=

# Nest API URL — обязателен для «Qabul qilish» / «Tayyor» и «Buyurtmani olish» после перезапуска бота.
# Должен совпадать с PUBLIC_API_URL на API сервисе. Не Vercel (фронт).
# Пример: https://your-api.up.railway.app
MINUTKA_API_URL=
# Альтернативное имя: NEST_API_URL=
